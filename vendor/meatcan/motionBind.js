/* 
 * Needs an object abstract
 * Needs an object class
 * Needs object clas instance which holds events 
 * Need to bind instances against displayItems.
 * 
 * 
 * 
 * 
 * 
 * 
 */

var motionBinder = (function() {

    var motionBinder = function() {
        var self=this;
    };

    motionBinder.prototype.add = function() { 
        
    };

    return motionBinder;
}());
